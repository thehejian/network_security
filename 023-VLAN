valn 
  vlan原理
    虚拟局域网，物理设备上连接的不收物理位置限制的用户逻辑组
    为什么用vlan
      交换机分割了冲突域，不能分割广播域
      随着交换机端口的增多，网络中广播增多，降低了网络的效率
      为了分割广播域，引入了vlan
  vlan的划分方式
  vlan帧格式
  PVID
  Access端口
vlan配置
  配置vlan的步骤
  创建删除vlan
  将端口加入vlan
  验证vlan配置
  
  
  
  
  
  
















